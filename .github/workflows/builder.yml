name: buildandtest

on: push

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Build1
        run: |
          cd lab1/
          make lab1test
          ./test
      
      - name: Build2
        run: |
          cd lab2/21
          make lab2test
          ./test
           cd ..lab2/22
          make test
          ./test
          

