name: buildandtest

on: push

jobs:

  build:

    runs-on: Ubuntu-20.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - run: |
          cd lab1/
          make lab1test
          ./lab_1_test
